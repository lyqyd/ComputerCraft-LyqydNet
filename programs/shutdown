if net.daemonTable.routed then coroutine.resume(net.daemonTable.routed, 0, "SI:stop") end
if net.daemonTable.netd then coroutine.resume(net.daemonTable.netd, 0, "SI:stop") end
print( "Goodbye" )
sleep( 1 )
os.shutdown()
