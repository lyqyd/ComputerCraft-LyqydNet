local oldpull = os.pullEvent
local oldrs = rednet.send

function newPull(filter)
	while true do
		local event = {oldpull()}
		if event[1] == "modem_message" then
			result = {net.message(event[3], event[5], event[6])}
			if result then
				return unpack(result)
			end
		elseif event[1] == "timer" and event[2] == net.networkDaemonTimeout then
			coroutine.resume(net.daemonTable.netd, 0, "SI:timeout")
		else
			if not filter or filter == event[1] then
				return unpack(event)
			end
		end
	end
end

function newSend(id, message)
	if net.routeFromCID(id) then
		net.packet_send("RM", net.routeFromCID(id), message)
	end
end

rawset(os, "pullEvent", newPull)
rawset(rednet, "send", newSend)

shell.run("socketd")